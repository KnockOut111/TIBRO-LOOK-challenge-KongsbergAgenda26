# Remember to fix for virtuel environment and ROS workspace when running the docker container.

services:
  nav_thread:
    build: 
      context: .
      dockerfile: navThread/Dockerfile
    container_name: roverpi-nav_thread
    privileged: true
    devices:
      - /dev/i2c-1:/dev/i2c-1
      - /dev/gpiomem:/dev/gpiomem
      - /dev/mem:/dev/mem
    volumes:
      - ./navThread:/app
    working_dir: /app
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app
      - BLINKA_FORCECHIP=BCM2XXX    
    command: python3 -u mainLogic.py  # Decides what is to run on startup, otherwise it will just run the default command which is to start a bash terminal

  metal_sensor_thread:
    build:
      context: .
      dockerfile: metalSensorThread/Dockerfile
    container_name: roverpi-metal_sensor_thread
    privileged: true
    network_mode: host
    devices:
      - /dev/gpiomem:/dev/gpiomem
    volumes:
      - ./metalSensorThread:/app
    working_dir: /app
    environment:
      - PYTHONUNBUFFERED=1
      - FASTDDS_BUILTIN_TRANSPORTS=UDPv4
    command: python3 -u metalSensorLogic.py

# IMU Possibly also wheel odometry data, but currently not implemented. Missing encoders for this to work?
  # imu_sensor_thread:
  #   build: .
  #   container_name: roverpi-imu_sensor_thread
  #   privileged: true
  #   devices:
  #     - /dev/i2c-1:/dev/i2c-1
  #   volumes:
  #     - .:/app
  #   working_dir: /app
  #   command: python imuSensorThread.py  # Needs to set what is to run when the container starts, otherwise it will just run the default command which is to start a bash terminal

# Stereo Camera thread for obstacle detection and avoidance. 
  # stereo_camera_thread:
  #   build: .
  #   container_name: roverpi-stereo_camera_thread
  #   privileged: true
  #   devices:
  #     - /dev/i2c-1:/dev/i2c-1
  #   volumes:
  #     - .:/app
  #   working_dir: /app
  #   command: python stereoCameraThread.py  # Needs to set what is to run when the container starts, otherwise it will just run the default command which is to start a bash terminal

# Running Visual SLAM, estimating the position (pose) of the rover and building a map of the environment. 
  # mapping_thread:
  #   build: .
  #   container_name: roverpi-mapping_thread
  #   privileged: true
  #   devices:
  #     - /dev/i2c-1:/dev/i2c-1
  #   volumes:
  #     - .:/app
  #   working_dir: /app
  #   command: python mappingThread.py  # Needs to set what is to run when the container starts, otherwise it will just run the default command which is to start a bash terminal

# Mainly handles sending of data to and from the remote server or computer
  # server_thread:
  #   build: .
  #   container_name: roverpi-server_thread
  #   privileged: true
  #   devices:
  #     - /dev/i2c-1:/dev/i2c-1
  #   volumes:
  #     - .:/app
  #   working_dir: /app
  #   command: python serverThread.py  # Needs to set what is to run when the container starts, otherwise it will just run the default command which is to start a bash terminal

# Posseble additional thread for finding the fastest path through the giving map in task 1. (RRT*)
  # finding_shortest_path_thread:
  #   build: .
  #   container_name: roverpi-finding_shortest_path_thread
  #   privileged: true
  #   devices:
  #     - /dev/i2c-1:/dev/i2c-1
  #   volumes:
  #     - .:/app
  #   working_dir: /app
  #   command: python findingShortestPathThread.py  # Needs to set what is to run when the container starts, otherwise it will just run the default command which is to start a bash terminal
